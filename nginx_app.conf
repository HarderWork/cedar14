location / {
    # try to serve file directly, fallback to rewrite
    try_files $uri $uri/ /index.php?$args @rewriteapp;
}

location @rewriteapp {
    # rewrite all to index.php
    rewrite ^(.*)$ /index.php/$1 last;
}

location ~ \.php$ {
    try_files @heroku-fcgi @heroku-fcgi;
    internal;
}